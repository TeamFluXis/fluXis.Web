{% extends "main/page.twig" %}
{% block content %}
<h1>Charts</h1>
<div class="maps">
    {% for map in maps %}
    <div>
        <h1>{{map.title}}</h1>
    </div>
    {% endfor %}
</div>
{% endblock %}